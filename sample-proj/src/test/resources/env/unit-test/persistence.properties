# Hibernate Settings
hibernate.hbm2ddl.auto=update
hibernate.dialect=org.hibernate.dialect.HSQLDialect
hibernate.show.sql=true
hibernate.format.sql=false

# Entity Models
entity.packages.to.scan=com.bskyb.db.entity
